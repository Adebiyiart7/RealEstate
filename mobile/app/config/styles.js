import colors from "./colors";

const defaultStyles = {
  colors: colors,
  primaryBorderRadius: 12,
  titleFont: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 10
  },
  gridStyle: {
    display: "flex",
    justifyContent: "center",
    marginTop: 16,
    marginHorizontal: -36,
    flexDirection: "row",
    flexWrap: "wrap"
  }
};

export default defaultStyles;
