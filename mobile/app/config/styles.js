import colors from "./colors";

const defaultStyles = {
  colors: colors,
  primaryRadius: 12,
  titleFont: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 10
  }
};

export default defaultStyles;
