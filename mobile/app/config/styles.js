import colors from "./colors";

const defaultStyles = {
  colors: colors,
  primaryBorderRadius: 12,
  titleFont: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 10
  }
};

export default defaultStyles;
